{
  "pre-build": [
    {
      "command": "cat $input | wasmer run --dir $mod_dir ../internal/cmd | moonfmt - > $output",
      "input": "input.mbt",
      "output": "output.mbt"
    }
  ],
  "import": [
    "moonbitlang/json_deriving"
  ]
}